const Main = ({
  title = 'React Boilerplate',
  description = 'ReactJS, TypeScript, NextJS & Styled Components',
}) => (
  <main>
    <h1>{title}</h1>
    <p>{description}</p>
  </main>
);

export default Main;
